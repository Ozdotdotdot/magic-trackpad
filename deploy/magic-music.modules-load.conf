# Magic Music: installed to /etc/modules-load.d/magic-music.conf
# Force the fork to load at boot (belt-and-suspenders alongside modalias autoload),
# so its host_click options are in effect before the trackpad connects.
hid-magicmouse-nexustar
